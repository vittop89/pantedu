---
tags:
  - documentazione/changelog
date: 2026-09-04
tipo: changelog-monthly
status: finale
aliases: ["changelog-2026-06"]
month: 2026-06
---

# Changelog Giugno 2026

Tutte le entry datate `2026-06-*`. Per index globale + altri mesi vedi
[changelog index](../changelog.md).

## 2026-06-05 — Hardening client: WAF JSON-per-XHR, CSRF centralizzato, de-jQuery, CSS-in-JS zero

Sessione di hardening + centralizzazione lato client/sicurezza. Vedi
[security-notes](../security-notes.md), [waf](../waf.md),
[docs/security/track6-7-csp-csrf-hardening.md](../../docs/security/track6-7-csp-csrf-hardening.md).

### WAF — risposte JSON per richieste XHR/API
- `WafMiddleware`: flag centralizzato `$expectsJson` (`wantsJson()` o path
  `/api/*`). Challenge e block ritornano **JSON 403** alle XHR
  (`{code:'waf_challenge',reload:true}` / `{error:'request_blocked'}`) invece
  della pagina HTML — **decisione di sicurezza invariata** (sempre 403,
  fail-closed). Fixa il crash `Uncaught SyntaxError: Unexpected token '<'` sulle
  `fetch`.
- Client: `dom-utils.assertJson` riconosce `waf_challenge` (JSON + HTML
  difensiva) e fa auto-reload one-shot (debounce 30s) per rinnovare il cookie
  `waf_session`.
- Test: `tests/Unit/WafMiddlewareJsonResponseTest.php`.

### CSRF — centralizzazione totale
- Unica fonte client `dom-utils.fetchCsrf` (cache 60s). Rimossi/aliasati ~tutti
  gli helper locali (`getCsrf`/`csrf`/`bsCsrf`/`_getCsrf`/`csrfToken`) in view
  inline, `tikz-*`, `checkin-handlers`, adapter pt-document, sidepage,
  `verifica-*`, `admin-*`. Unico `fetch('/auth/csrf')` rimasto = canonico in
  `dom-utils.js`.

### De-jQuery / de-shim
- Eliminato `js/modules/core/ajax-compat.js` (`$.ajax`-like `done/fail/always`):
  8 moduli migrati a `fetch`/`fetchJson` vanilla (`then/catch/finally`):
  `dom-manager`, `content-processor`, `editor-system`, `utilities`,
  `event-handler`, `google-apps-script`, `ui-comp`, `table-manager`.
- Confermato zero jQuery nel runtime (libreria non caricata; `$` = alias
  `querySelector`).

### CSS-in-JS azzerato (ADR-023 Fase 5)
- Ultima iniezione runtime (`bootstrap-compat.injectSyncStyles`) → modulo
  `css/modules/_sync-status.css` (import in `components.css`). CI `no-css-in-js`
  verde.

### Profilo docente + componente autocomplete
- Fix area `/area-docente/profilo`: tab Classi/Materie (bug `.fm-d-none`),
  selettore classe scopato all'istituto attivo, feedback "Già collegato"
  onesto, nome MIUR ufficiale al posto dell'`header_label`.
- Nuovo componente vanilla riusabile e accessibile (WCAG 2.2 AA)
  `js/modules/ui/autocomplete.js` (combobox a tema dark) — sostituisce il
  `<datalist>` nativo.

### Audit `/admin` (esito positivo, nessuna modifica)
- Routing dietro `auth + role:admin`; mutazioni in gruppi `csrf+rate`;
  operazioni sensibili `super_admin_required` (+ `audit_reason`); gruppi senza
  CSRF contengono solo GET. SQLi coperto (prepared + whitelist
  `/admin/logs/api/{table}`); XSS coperto (`esc`/`escAttr`; nessun `<?= $var ?>`
  grezzo). `/api/admin/*` in `shouldBypass` del WAF.

### Gate
- `no-css-in-js`, `csp:no-inline-handlers`, `eslint --quiet` (0 errori),
  `vite build` — tutti verdi.

> [!info] Le voci che seguono sono state scritte il 2026-09-04 dai commit
> (`git log --since=2026-06-06`), durante l'aggiornamento della wiki chiesto
> dalla revisione architetturale di settembre (sezione 7, voce 1). Fino ad
> allora la wiki si fermava al 5 giugno.

## 2026-06-10 — PDF-Import — estrazione in background, difficoltà, modelli per operazione, configurazione privata per docente

Causale: il tool di estrazione esercizi da PDF (reimplementazione del vecchio
strumento Python) copriva solo l'estrazione base; in una settimana ha raggiunto
la parità con il legacy e ha preso una configurazione per docente.

| Ambito | Cosa | Commit |
|---|---|---|
| Estrazione | in background con log live e pulsante Stop; stato `cancelled` in migration 090; un blocco per esercizio all'insert; retry rapido + backoff | `27687e9`, `d01fea1`, `4cb7c6d`, `9425888` |
| Difficoltà | analisi pixel dei pallini (GD, niente LLM), poi agente LLM a pagina intera, automatica a fine estrazione | `f898bd0`, `7f663ab`, `109d701` |
| Argomento, traduzione, soluzioni | TopicAgent, traduzione EN→IT, soluzioni AI per categoria incrementali, auto-correzione V/F | `ba07104`, `d30f580`, `0d5b675`, `9f2d9cc` |
| Modelli e prompt | modello e prompt per operazione, pagina `/area-docente/pdf-import/models`, preset globale + override per docente | `7383785`, `704381e`, `cf75b27` |
| Configurazione privata | chiavi provider cifrate con la KEK del docente, cache LLM con toggle, lock per sessione su `contracts.json` | `3285c8f`, `b37c591`, `248bbb8` |
| Privacy | avviso GDPR per i provider cloud, con invito a Ollama locale | `596043f` |
| Codice | `JsonConfigStore` base per i tre store per docente; `LlmAuditLog::ms()`; parsing JSON LLM centralizzato | `52ee7f4`, `1be1c0a`, `a52cd78` |

File: `app/Controllers/Teacher/PdfImportController.php`, `app/Services/PdfImport/**`,
`js/entries/pdf-import.js`, `js/entries/pdf-import-models.js`, `database/migrations/090_*.sql`.

## 2026-06-09 — Rotte — pagine docente unificate sotto `/area-docente/*`

Causale: le pagine del docente vivevano su `/teacher/*` e `/area-docente/*`
insieme. Ora la pagina è una sola; `/teacher/*` resta come redirect 302 per i
segnalibri, le API restano `/api/teacher/*`. Commit `edab593`. File:
`routes/web.php`, `app/Controllers/TeacherProfileController.php`.

## 2026-06-14 — Risdoc — editor PT: tabelle, sezioni, gruppi dinamici, valori per terna (ADR-030), formule (ADR-031)

Causale: i modelli risdoc resi dal componente unificato dovevano coprire i
casi reali dei documenti (piano annuale, relazioni) senza duplicare un
documento per ogni classe.

| Ambito | Cosa | Commit |
|---|---|---|
| Tabelle | inserimento righe/colonne direzionale, unione celle, larghezze, allineamento, celle rich (B/I/U/code), tipo cella Checkbox, corsia ⚙ | `1417839`, `7001d80`, `39f8357`, `dede86d`, `330a0ae`, `8790aa8` |
| Sezioni | sposta ↑/↓, duplica, elimina con conferma, checkbox 👁 «In output», «☑ tutte», accordion con voci escludibili | `f661dbb`, `7ed6a4d`, `4604029`, `e5a3673`, `a4d57c4` |
| Gruppi e sorgenti | gruppi di checkbox da sorgente JSON, cascata sorgenti tipo → Automatico/file, «Automatico» inserisce un gruppo collegato che segue i selettori | `437f1ce`, `cb5ebfb`, `2b4111d`, `bc78d93` |
| ADR-030 | un documento, valori per terna indirizzo/classe/materia: toggle 🔗/📌 per gruppo/campo, render server per terna, salvataggio de-idratato | `36e4c3a`, `4b17c5b`, `67f7fe6`, `c5fd1e8` |
| ADR-031 | motore formule (JS + specchio PHP `FormulaEngine`), barra della formula, funzioni IT/EN, drag-to-fill, formule inline | `f143313`, `71b6283`, `b3db93c`, `9a16a72`, `436ab8f` |
| Export TeX | compile dei documenti da modello dal `body_pt` (ADR-026), intestazione e numerazione dal layout master (sezioni non numerate nel corpo), tabelle con caption senza float, checkbox in cella incolonnate | `c02b01b`, `837c1c9`, `c0777d3`, `4179b5b` |
| Sorgenti curriculari | nuovo JSON «Competenze trasversali di cittadinanza»; editor dei file sorgente in `/admin/templates` (tab «Sorgenti JSON») | `002ae41`, `090b021` |

File: `js/components/risdoc/fm-risdoc-pt-editor.js`, `js/modules/risdoc/pt/pm-schema.js`,
`js/modules/risdoc/pt/formula-engine.js`, `app/Services/Risdoc/Pt/FormulaEngine.php`,
`app/Services/Risdoc/Pt/TernaBinding.php`, `app/Services/Risdoc/Pt/PtToTex.php`,
`app/Controllers/Admin/RisdocAdminController.php`. Decisioni: [[decisions/ADR-030-document-terna-scoped-values]],
[[decisions/ADR-031-table-formulas]]. Nota: il commit `837c1c9` è la causa di
sette test unitari poi marcati `TEST_ROT` (vedi voce del 2026-08-31).

## 2026-06-11 — ADR-029 — decomposizione dei God-controller e documentazione generata

Causale: `TeacherContentController` (1.886 righe) e `ContentStudyController`
(1.883) mescolavano CRUD, pubblicazione, export, quesiti, gruppi e template.

- Estratti `ContentExportController`, `ContentPublishController`,
  `QuesitoController`, `GroupController`, `ContentTemplateController`,
  `StudyHeaderController`, `StudySourcesController` e il servizio
  `Risdoc\TemplateDefaults`; `TeacherContentController` a 484 righe
  (`5bb7f6e`, `9a686e0`, `c3903ad`, `dca91bf`, `f13e7c4`, `011a4d3`, `e40d6b1`, `5aacf1b`).
- Strumenti: `tools/dev/extract_controller.php`,
  `tools/dev/check_controller_complete.php`; hook che rigenera `docs/ROUTES.md`
  quando cambia `routes/web.php` (`9c093e4`).
- Documenti generati o nuovi: `docs/ROUTES.md`, `docs/SERVICES.md`,
  `docs/PHASES.md`, `ARCHITECTURE.md`, `docs/VALIDATION.md`, method-map in
  `docs/glossary/` (`2a5f44d`, `aa77e53`, `d0104ba`, `8fde5a5`, `fcac84e`).
- Qualità: sweep PSR-12 con `phpcbf`, baseline PHPStan rigenerata, fine riga
  LF forzata per i `.php` (`7198820`, `7acac58`, `9e2bc8e`).

Decisione: [[decisions/ADR-029-decomposizione-god-controller]].

## 2026-06-15 — Sicurezza — audit del 14 giugno (FND-008…011)

Causale: secondo giro di audit assistito dopo il pentest di maggio.

| Rilievo | Rimedio | Commit |
|---|---|---|
| FND-008 | CSRF sul form pubblico di contatto DPO | `40f6e79` |
| FND-009 | rotte di stato da `any()` a `post()` (niente verb tampering) | `d639c76` |
| FND-010 | `saveImage` applica `SvgSanitizer` completo | `c2106d5` |
| FND-011 | `\input` di file arbitrari bloccato nel compile TeX | `8660f8c` |
| Dipendenze | Guzzle con CVE aggiornato, dipendenze TeX, sink XSS latente | `6d8b960` |

## 2026-06-18 — Registrazione — modalità dati studente (completa, ridotta, anonima) e classi ammesse

Causale: preparazione al parere del DPO: l'istanza deve poter raccogliere meno
dati dagli studenti e limitare le classi che possono iscriversi.

- Modalità `full/reduced/anonymous` in `App\Support\StudentRegistration`,
  con override runtime `storage/config/student_registration.json` e pannello
  `/admin/system/deployment` (`2322459`).
- Visibilità dello studente ancorata all'account e scoping per istituto
  (`1a4fe39`); «Classi ammesse» con selettori a cascata istituto → indirizzo →
  classe (`a418837`).
- Clone «Solo fonte» degli esercizi dalle verifiche: traccia → segnaposto,
  soluzioni → «...» (`a41b2fe`, `1815e02`, `5d3f0a3`).

## 2026-06-20 — Accesso pubblico — sezioni `publish_public`, endpoint dedicati, CSP strict sulla home

Causale: mostrare a chi non ha un account i contenuti pubblicati dal
super-admin senza aprire le API autenticate.

- Colonna «docenti» delle sezioni a tre modalità e flag «pubblica
  superadmin» (`5dfd7cd`, `1b1c663`); render pubblico
  `GET /public/sidebar/{key}` (`83195b3`).
- Endpoint pubblici dedicati `/api/public/study/*` e vista
  `/public/studio/{id}` con gate per sezione e rate limit
  (`7f4e4d6`, `52a4284`, `a2f63ac`, `2cf1dc0`); guest con curriculum e
  contenuti del super-admin (`f01e484`).
- La home passa dal Kernel: CSP `strict` anche sulla landing (`5d3630d`);
  via `href="javascript:"` dal pannello WAF (`5420a84`).

File: `app/Controllers/ContentStudyController.php` (metodi `public*`),
`app/Controllers/PublicSidebarController.php`, `app/Repositories/SidebarSectionRepository.php`.

## 2026-06-21 — Accessibilità — WCAG 2.2 AA come gate CI, MathML assistivo, dichiarazione conforme

Causale: la dichiarazione AgID doveva passare da «parzialmente conforme» a
«conforme» con prove ripetibili.

- Suite axe estesa a `wcag22a/wcag22aa` e alle pagine autenticate; il job
  `a11y.yml` fallisce su violazioni serious/critical (`0bda5de`, `694c9d7`, `25a905e`).
- Formule con MathML assistivo (`2c397a8`); target size ≥ 24 px; builder
  verifiche operabile da tastiera; nomi accessibili; contrasto trust page
  (`552130e`, `ac10db7`, `1298f1d`, `34c9c3f`).
- LHCI contro il server locale, non la produzione (il WAF blocca i bot)
  (`9ad6d87`); CI a11y con symlink css/js per testare la pagina stilizzata (`3d60ce1`).
- Service worker: purga cache al logout, pagine auth-dipendenti network-only,
  auto-reload al cambio di controller (`fd3efbb`, `dce0c79`, `e049478`).

Documenti: `docs/legal/accessibility.md`, `docs/legal/accessibility-audit.md`.

## 2026-06-25 — Pubblicazione — sanitizzazione a publish-time, codice pubblico dal 23 giugno, pacchetto per il DPO

Causale: candidatura al catalogo Developers Italia e apertura del repository.

- `tools/publish/sanitize-for-publication.php`: rimozione di segreti, dati
  personali, log runtime, binari con dati reali, cartelle interne; super-admin
  parametrico; regola sulle email dell'operatore (`e1e6efe`, `360e62c`,
  `ab04b3a`, `ce71463`, `38481c6`).
- `publiccode.yml` conforme al parser ufficiale (`e424778`, `cff250a`);
  template issue/PR (`d5845d5`); guida esplicita alla creazione del
  super-admin in `docs/INSTALL.md` (`72e6df2`).
- Pacchetto di accountability GDPR per il DPO di un istituto: bozza di DPA
  art. 28, lettera, executive summary, tre scenari d'uso (personale,
  colleghi, istituto) (`9e115f8`, `646fede`, `ce35358`). Vedi `docs/dpo/`.
